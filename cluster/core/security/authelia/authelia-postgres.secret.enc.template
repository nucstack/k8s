# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: authelia-postgres-helm-values
  namespace: security
type: Opaque
stringData:
  values.yaml: |
    postgresqlPassword: ${AUTHELIA_POSTGRES_PASSWORD}
