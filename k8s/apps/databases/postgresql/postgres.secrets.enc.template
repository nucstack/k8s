# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-helm-values
  namespace: databases
stringData:
  values.yaml: |
    global:
      postgresql:
        auth:
          postgresPassword: "${POSTGRES_PASSWORD}"
