---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: zomboid
  namespace: games
spec:
  interval: 5m
  chart:
    spec:
      chart: k8s/charts/kah-common/
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 5m
  valuesFrom:
    - kind: Secret
      name: zomboid-helm-values
  values:
    image:
      repository: openjdk
      tag: "20"
    controller:
      replicas: 1
    nameOverride: zomboid
    initContainers:
      download:
        name: download
        image: steamcmd/steamcmd:alpine
        command: ["steamcmd"]
        args: [
          "+login",
          "anonymous",
          "+force_install_dir",
          "/data",
          "+app_update",
          "380870",
          "+quit"
        ]
        volumeMounts:
          - name: data
            mountPath: "/data"
    persistence:
      data:
        enabled: true
        mountPath: /data
        size: 10Gi
        storageClass: longhorn
        accessMode: "ReadWriteOnce"
      settings:
        enabled: true
        type: custom
        volumeSpec:
          configMap:
            name: zomboid-settings
        subPath:
          - path: test.ini
            mountPath: /root/Zomboid/Server/test.ini
          - path: test_SandboxVars.lua
            mountPath: /root/Zomboid/Server/test_SandboxVars.lua
          - path: test_spawnpoints.lua
            mountPath: /root/Zomboid/Server/test_spawnpoints.lua
          - path: test_spawnregions.lua
            mountPath: /root/Zomboid/Server/test_spawnregions.lua
    command: /data/start-server.sh
    podAnnotations:
      secret.reloader.stakater.com/reload: zomboid-settings
    service:
      main:
        type: LoadBalancer
        loadBalancerIP: "${SECRET_ZOMBOID_LB_IP}"
        externalIPs:
          - "${SECRET_ZOMBOID_LB_IP}"
        externalTrafficPolicy: Local
    resources:
      requests:
        cpu: 500m
        memory: 1Gi
    probes:
      liveness:
        enabled: false
      readiness:
        enabled: false
      startup:
        enabled: false
