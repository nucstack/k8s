# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
  name: gitlab-helm-values
  namespace: development
stringData:
  values.yaml: |
    global:
      appConfig:
        ldap:
          enabled: true
          servers:
            main:
              label: "LDAP"
              host: "${LDAP_SERVER}"
              port: 636
              encryption: "simple_tls"
              uid: "uid"
              bind_dn: "uid=${LDAP_ADMIN_USERNAME},cn=users,${LDAP_ROOT}"
              base: "cn=users,${LDAP_ROOT}"
              user_filter: "(memberOf=cn=gitlab,ou=groups,${LDAP_ROOT})"
              active_directory: false
              password:
                secret: gitlab-ldap-password
---
apiVersion: v1
kind: Secret
metadata:
  name: gitlab-ldap-password
  namespace: development
stringData:
  password: "${LDAP_ADMIN_PASSWORD}"
