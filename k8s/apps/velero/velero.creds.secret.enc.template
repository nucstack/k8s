# yamllint disable
apiVersion: v1
kind: Secret
type: Opaque
metadata:
    name: velero-creds
    namespace: velero
stringData:
  cloud: |
    [default]
    aws_access_key_id=${MINIO_ACCESS_KEY}
    aws_secret_access_key=${MINIO_SECRET_KEY}
