---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: basic-auth
  namespace: networking
spec:
  basicAuth:
    secret: traefik-basic-auth # checkov:skip=CKV_SECRET_6 secret ref
