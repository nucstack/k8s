# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: global-secrets
    namespace: flux-system
stringData:
    SECRET_DOCKER_PRIVATE_REGISTRY: ${DOCKER_PRIVATE_REGISTRY}
    SECRET_CLOUDFLARE_EMAIL: ${CLOUDFLARE_EMAIL}
    SECRET_NFS_HOST: ${NFS_HOST}
    SECRET_NFS_SHARED_PATH: ${NFS_SHARED_PATH}
    SECRET_NFS_PHOTOS_PATH: ${NFS_PHOTOS_PATH}
    SECRET_NFS_VIDEOS_PATH: ${NFS_VIDEOS_PATH}
    SECRET_ALERTMANAGER_SLACK_WEBHOOK: ${ALERTMANAGER_SLACK_WEBHOOK}
    SECRET_K3S_HA_VIP: ${K3S_HA_VIP}
    SECRET_DNS_SERVER: ${DNS_SERVER}
    SECRET_NEXTDNS_ID: ${NEXTDNS_ID}
