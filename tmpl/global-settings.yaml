---
apiVersion: v1
kind: ConfigMap
metadata:
  name: global-settings
  namespace: flux-system
data:
  NETWORK_K8S_CLUSTER_CIDR: "${K3S_CLUSTER_CIDR}"
  NETWORK_K8S_SERVICE_CIDR: "${K3S_SERVICE_CIDR}"
