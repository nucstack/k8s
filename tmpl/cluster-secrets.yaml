# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: cluster-secrets
    namespace: flux-system
stringData:
    SECRET_DOMAIN: ${DOMAIN}
    SECRET_LOCAL_DOMAIN: ${LOCAL_DOMAIN}
    SECRET_NFS_BACKUP_PATH: ${NFS_BACKUP_PATH}
    SECRET_NFS_PROMETHEUS_PATH: ${NFS_PROMETHEUS_PATH}
    SECRET_INFLUXDB_USER: ${INFLUXDB_USER}
    SECRET_INFLUXDB_PASSWORD: ${INFLUXDB_PASSWORD}
    SECRET_RSYSLOG_LB_IP: ${RSYSLOG_LB_IP}
    SECRET_MQTT_LB_IP: ${MQTT_LB_IP}
    SECRET_METALLB_LB_RANGE: ${METALLB_LB_RANGE}
    SECRET_TRAEFIK_LB_IP: ${TRAEFIK_LB_IP}
    SECRET_MQTT_ADMIN_PASSWORD: ${MQTT_ADMIN_PASSWORD}
    SECRET_MQTT_USER1_USERNAME: ${MQTT_USER1_USERNAME}
    SECRET_MQTT_USER1_PASSWORD: ${MQTT_USER1_PASSWORD}
